{
  "name": "whatsapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd client && npm install",
    "server-install": "cd server && npm install",
    "socket-install": "cd socket && npm install",
    "install-all": "npm run client-install && npm run server-install && npm run socket-install",
    "client": "cd client && npm start",
    "server": "cd server && npm run start",
    "socket": "cd socket && npm run start-socket",
    "start-all": " concurrently \" npm run client \" \" npm run server \" \" npm run socket \" "
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
